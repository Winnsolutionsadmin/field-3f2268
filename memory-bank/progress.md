---
name: progress — field-3f2268
description: What works, what's broken, what's queued. Source of truth for project state.
type: project
---

# Progress — field-3f2268

## What works

- Published belief field for John Wu is live on GitHub Pages: landing (`index.html`), 3D belief map (`john-wu.html`), belief database (`belief-explorer.html`), backed by `data/*.js`.

## What's broken / known issues

(None known.)

## What's queued

- Nothing queued. Artifact appears complete. Reopen only if the John Wu engagement requires updates.

## What's out of scope

<!-- Be explicit about what is NOT part of this project. Saves arguments later. -->

- Hand-editing the generated HTML/data — regenerate via the `/belief-field` skill instead.

## Done

- 2026-06-02 — retrofitted to the canonical scaffold (CLAUDE.md + memory-bank) via /retrofit-scaffold.
- (earlier) — belief field built and published (commit `7b9b2fe publish`) to `github.com/Winnsolutionsadmin/field-3f2268`.
